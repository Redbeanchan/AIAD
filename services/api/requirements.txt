# AIAD API – background removal (pretrained model)
# Install: pip install -r services/api/requirements.txt
# Move this file to: AIAD/services/api/requirements.txt

fastapi>=0.100
uvicorn[standard]>=0.22
python-multipart>=0.0.6

# rembg (isnet-general-use) – [gpu] uses onnxruntime-gpu (CUDA) for NVIDIA GPU
rembg[gpu]>=2.0
pillow>=10.0
